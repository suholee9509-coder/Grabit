/**
 * annotation 엔티티 배럴 — ★표현 전용 목 타입(DB annotations 테이블/계약과 무관).
 * DM1 옵션1: 우측 사이드바 댓글/답글은 픽셀퍼펙트 UI + 목킹(BE 미연결). 실 엔티티는 다음 스프린트.
 */
export type { MockComment, MockReply } from './model/types';
export { DEMO_COMMENTS } from './model/demo-comments';
