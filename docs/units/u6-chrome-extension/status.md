# u6-chrome-extension — status

> dev가 매 턴 갱신(변경·검증결과·리스크). PM은 이 파일 + STATUS 반환으로 통합 결정.

- 상태: **계획(미스폰)**
- 검증: (미실행 — 스폰 후 dev가 채움)
- 변경 파일 (boundary 준수): (없음)
- 설계 노트:
  - 무거운 단위(MV3). 메인 spec = 확장 골격 + content 주입 + 클립 모달. 무거우면 폴더 내 서브-spec 분할(auth/ingest/install — 티켓 증식 ❌).
  - dep: u0-design-system(토큰·shared/ui) + **u0b-data-core(ingest 계약·정준키·clips 스키마) 머지 후**. 신규 BE/마이그레이션 없음 — u0b 계약 소비만.
  - 타임코드 칩(82×38 radius4) = 클립 모달 전용 위젯, u0c 공용 범위 밖 → 이 유닛에서 정의.
  - 영상 전용(게이트 ⓐ): 아티클 클리핑/하이라이트·AI·대시보드·결제 제외.
- 리스크:
  - Step1/Step2(드롭다운 발견·툴바 핀)는 브라우저 크롬 UI 소유 → 확장이 직접 렌더 불가. 안내·핀 affordance만 구현 가능(충실도 평가 범위 조정 필요).
  - MV3 SW sleep 시 토큰 갱신·세션 유지 위협모델 = auth 서브-spec에서 확정.
  - u0b ingest 계약이 folder_id·tags·is_public·중복응답을 충분히 노출하는지 미검증(스폰 시 대조).
- ESCALATION (PM 결정 대기 — 게이트 ⓑ 전 정리):
  1. **클립 모달 vs 기획 충돌**: Step4 모달은 공개범위·폴더·태그까지 갖춘 *리치 모달*인데, 기능명세서/PRD의 확장 클립은 '플로팅 메모 입력창'(500자·엔터/ESC·3초 자동저장) 수준. → 확장 클립 UI는 **Step4(무엇=Figma 우선)** 로 가되, 메모 규칙(S-HEWHTN)은 모달 메모 입력에 흡수하는 것으로 가정. PM 확인 필요.
  2. **메모 없이 구간만 저장 허용?** 빈 메모로 [완료] 가능 여부 = 디자인 공백. u0b 계약상 memo nullable인지 + 기획('메모 입력' 전제)과 정합 확인.
  3. **저장 폴더 드롭다운의 폴더 출처**: 폴더 CRUD는 u7(라이브러리). 확장은 기존 폴더 목록을 읽어 부착만 한다고 가정 — u0b/u7 폴더 read 경로 확정 필요.
  4. **빈/로딩/에러/저장중/미인증/중복/popup** = 디자인 공백 → u0 파운데이션(토스트·스피너·버튼 상태)로 채움. popup 단독 디자인 부재 → 최소 셸 승인 필요.
  5. Sprint/Wave 번호·Issue # 미정 — PM이 게이트 ⓑ에서 확정.

STATUS = "계획(미스폰): u6 spec/status 스텁 작성 완료 · 5프레임 역설계 · dep=u0+u0b · ESCALATION 5건 PM 대기"
