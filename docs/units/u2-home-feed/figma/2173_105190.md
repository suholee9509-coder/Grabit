# Figma 실측 — 홈/취향관 최종 시안 · 카테고리칩 active 상태

- **fileKey**: `5GGyKsjXEOpjKMLtUodeSs`
- **대상 nodeId**: `2173:105190` (`홈_취향관_최종 시안`, FRAME)
- **렌더 PNG**: `2173_105190.png` (3840×2176, scale 2) · 칩행 크롭 `2173_105190_chiprow.png` (3202×300, scale 2)
- **역할(이 문서의 초점)**: 취향관 카테고리칩 **active(선택) 상태** 측정 — 선택 칩의 그린 링 + 그린 라벨 변형.
- **렌더 확인(정본)**: 칩행 실제 텍스트 순서 = 내 분야 · 프로그래밍 · UXUI 디자인 · 업무 생산성 · IT 기획 · **리더십(ACTIVE)** · 협업 · 분야 추가. ⚠ 작업지시의 "크로스 트렌드"라는 칩명은 실제 렌더에 없음 → **렌더가 SoT이므로 active 칩 라벨 = "리더십"**으로 기록. (active 변형의 스타일 추출이 목적이며 그 부분은 동일.)

> 이 프레임은 홈/취향관 전체 화면(1920×1088 뷰포트, 내부 스크롤 콘텐츠 1601×5290)이다. 본 문서는 **카테고리 칩행(2차 내비)** 의 active 상태를 정밀 측정하고, 주변 컨텍스트(1차 세그먼트 내비·프레임 레이아웃)는 active 변형 이해에 필요한 만큼만 기록한다.

---

## 1. 프레임 전체 레이아웃

- **루트 프레임** `2173:105190` (layout_4PLQ9W): 1920×1088, `overflowScroll: y`, fill `#000000` (`fill_X8T4SQ`) → 토큰 `--color-bg`. (앱 셸 뷰포트)
- **콘텐츠 컬럼** `2173:105191` (Frame 2085668465, layout_V9UR68): x=270, y=8, **width 1601** × height 4798 (스크롤 콘텐츠). 좌측 GNB(254px) 뒤 본문 폭 = 1601. 토큰 갭 없음(레이아웃 폭).
- **본문 좌 inset 기준선**: 섹션 제목·칩행 등은 콘텐츠 컬럼 내부에서 x≈26~40 정렬.
- 좌측 사이드바 `2173:106540` (Sidebar 인스턴스): width 254 → 토큰 `--size-gnb`. 프로필카드 226×50 → `--size-gnb-panel`·`--size-profile-card-h`. (컨텍스트만)

### 1-A. 1차 내비 세그먼트 (취향관 / 피드) — `2173:105355`
active 칩행 바로 위의 1차 토글. 칩 active와 혼동 방지로 함께 기록.
- 컨테이너(layout_UAQQCZ): padding 4px, gap 10px, radius **100px** (`--radius-pill`), fill `#1B1B1B` (`fill_R46FCM`) → `--color-surface-segment`. 위치 x=40, y=514(콘텐츠 컬럼 기준).
- 탭 item(layout_4WEY0C): padding `4px 12px 4px 10px`, gap 10px, radius 100px.
  - **선택 탭 "취향관"**: fill `#363636` (`fill_MDOJSA`) → `--color-surface-tab-selected`; 라벨 `Body 2/Semibold` (Pretendard / 600 / 15px / lh160% / ls-2%) 색 `#FAFAFA` (`fill_AEQ21D`) → `--color-text-primary`.
  - **비선택 탭 "피드"**: fill 없음(투명); 라벨 `Body 2/Medium` (Pretendard / 500 / 15px / lh160% / ls-2%) 색 `#999999` (`fill_2S73IF`) → `--color-text-tertiary`.
  - 아이콘 16×16 (layout_GMADXN).
  - 타이포 매칭: `--text-segment-lg-size:15px` / `--text-segment-lg-line:160%`.

---

## 2. 카테고리 칩행 (2차 내비) — 컨테이너 `2117:32039` (= `2173:105356 → 2117:32039`)

- **칩행 컨테이너** `2117:32039` (Frame 2085667719, layout_GSX3WS): 1601×150. 부모 `2173:105356`(layout_XHEIF0)는 콘텐츠 컬럼 기준 x=0, y=586, 1601×150.
- **칩 트랙** `2117:32040` (Frame 2085667731, layout_EJL1HA): `mode: row`, **gap 30px** (칩 간격), hug. 트랙 시작 위치 x=367, y=20 (컨테이너 내부, 좌측 그라데이션 페이드 후).
  - 토큰 갭: gap 30px = `[토큰갭]` (spacing 토큰에 30px 없음; 최대 24/28).
- **개별 칩** (예: `2117:32041` Frame 2085668996, layout_KJBBDI): `mode: column`, `alignItems: center`, **gap 10px** (아바타↔라벨) → `--space-5`. hug.

### 2-A. 칩 아바타(원형 썸네일) — 공통

- **아바타 박스** (예: Group 1707483500 `2117:32042`, layout_LOXDV2): **82×82** → `--size-avatar-xl`.
  - 내부 마스크그룹(layout_0HDYKO) 82×82.
  - **Ellipse 962**(예 `2117:32044`): 82×82 원. 기본 fill `#D9D9D9` (`fill_V5MQCP`) [플레이스홀더] 또는 `#141414` (`fill_PBCBEX`, 프로그래밍 칩) [≈`--color-surface`계열, 정확히는 토큰갭]. stroke `#000000` (`fill_DMJCWE`) 1px (썸네일 외곽 검정 테두리).
  - **3D 아이콘 이미지**(RECTANGLE, IMAGE fill, scaleMode STRETCH): 칩별 이미지 imageRef. (콘텐츠 이미지 — 픽셀 측정 대상 아님, 자산.)
- **칩 테두리 링**(기본/비활성) — 예 `2117:32046` Rectangle 3466252 (layout_0HDYKO 82×82, radius **100px** → `--radius-pill`):
  - stroke `rgba(255, 255, 255, 0.08)` (`fill_E3SEC3`) → `--color-border-subtle`, **strokeWeight 1px**.
  - 일부 칩은 동일 링이 `Frame 2085669042`(layout_8TN8EO, 82×82, padding 8px, radius 100px) 형태 — stroke·weight 동일(0.08 / 1px).

### 2-B. 칩 라벨 — 공통

- 라벨 프레임(예 `2117:32047` Frame 2085667722, layout_ATROEY): column, gap 10px, hug.
- 텍스트(예 `2117:32048` "내 분야", layout_6E43IZ, fill 가로): `style_N4CHHJ` = **Pretendard / SemiBold(600) / 14px / lineHeight 130% / letterSpacing -2.5% / textAlign CENTER**.
  - 토큰 매칭: size 14 → `--text-body-3-size`(14)지만 lh130%는 body-3(20px=143%)와 상이; 정확히는 `[토큰갭]`(14/600/lh130%/ls-2.5%/center — 카테고리 칩 라벨 전용, 토큰 type ramp에 부재). family `--font-family-base`, weight `--font-weight-semibold`, ls `--letter-spacing-tight`.
- **기본(비활성) 라벨 색**: `#B4B4B4` (`fill_2WWDSW`) → `--color-text-secondary` / `--color-gray-500`.

---

## 3. ★ ACTIVE(선택) 칩 — "리더십" `2117:32081` (Frame 2085669001) / `2117:32082`

active 변형은 **링 + 라벨 색** 두 가지가 기본과 다르다. 칩 박스 치수·레이아웃은 기본과 동일.

| 속성 | 기본(비활성) | **ACTIVE(선택)** | nodeId |
|---|---|---|---|
| 칩 컬럼 layout | column, center, gap 10px | 동일 | active 칩 `2117:32082` (layout_KJBBDI) |
| 아바타 박스 | 82×82 | 82×82 | Group 1707483495 `2117:32083` (layout_LOXDV2) |
| 아바타 Ellipse fill | `#D9D9D9` 등 | `#D9D9D9` (`fill_V5MQCP`) — 동일(자산 이미지 위) | Ellipse 962 `2117:32085` |
| **선택 링 stroke 색** | `rgba(255,255,255,0.08)` (`fill_E3SEC3`) | **`rgba(102, 255, 75, 0.5)`** (`fill_T3EFM2`) | 링 Frame 2085669042 `2117:32087` |
| **선택 링 두께** | 1px | **2px** | `2117:32087` (strokeWeight 2px) |
| 링 박스/radius | 82×82, radius 100px | 82×82, radius **100px** (`--radius-pill`), padding 8px (layout_8TN8EO) | `2117:32087` |
| **라벨 색** | `#B4B4B4` (`fill_2WWDSW`) | **`#66FF4B`** (`fill_KDWMQ4`) | 텍스트 "리더십" `2117:32088` |
| 라벨 타이포 | style_N4CHHJ (14/600/130%/-2.5%/center) | 동일 style_N4CHHJ | `2117:32088` |

### active 토큰 매칭
- 선택 링 색 `rgba(102, 255, 75, 0.5)` → **`--color-brand-primary-50`** (정확 일치, 주석에 "카테고리 선택 링" 명시).
- 선택 링 두께 2px → **`--size-avatar-ring`** (정확 일치, "카테고리 선택 링 두께").
- 링 radius 100px → `--radius-pill`.
- 라벨 색 `#66FF4B` → **`--color-brand-primary`** (정확 일치).
- 아바타 82×82 → `--size-avatar-xl`.

### 발견된 토큰갭 (active 관련)
- **칩 라벨 타이포 셋(14 / 600 / lh130% / ls-2.5% / center)** — type ramp에 1:1 토큰 없음(body-3는 lh가 다름) → `[토큰갭]`. (active/기본 공통)
- **칩 간 gap 30px** — spacing 스케일에 30px 부재 → `[토큰갭]`.
- 아바타 Ellipse 플레이스홀더 색 `#D9D9D9` / 칩별 `#141414` — 색 토큰 부재(자산 베이스라 영향 작음) → `[토큰갭]`.

> 정리: active 변형의 **핵심 두 값은 토큰에 정확히 존재** (`--color-brand-primary-50` 링색 · `--size-avatar-ring` 2px · `--color-brand-primary` 라벨). 따라서 active 구현은 토큰만으로 픽셀-퍼펙트 가능. 칩 라벨 타이포와 칩간 gap만 토큰갭으로 별도 처리 필요.

---

## 4. 칩행 "분야 추가(+)" 칩 — `2117:32097` (Frame 2085669003)

active 아님(참고). 
- 링 `Frame 2085669042` `2117:32098` (layout_7S11KM): 82×82, padding 8px, radius 100px, stroke `rgba(255,255,255,0.08)` (`fill_E3SEC3`) **1px** → `--color-border-subtle`. 채움 없음(자산 이미지 없이 + 아이콘만).
- **+ 아이콘** `2117:32099` (아이콘_더하기, IMAGE-SVG, componentId 675:520): **32×32** (layout_9DH8Y3).
- 라벨 "분야 추가" `2117:32101`: `style_UL53DH` = Pretendard / **Medium(500)** / 14px / lh130% / ls-2.5% / center; 색 `#FAFAFA` (`fill_CM8GYZ`) → `--color-text-primary` / `--color-white`. (다른 칩 라벨은 SemiBold·gray, 이 칩만 Medium·흰색.)

---

## 5. 칩행 좌/우 그라데이션 페이드 (스크롤 어포던스)

칩행은 가로 스크롤 컨테이너로, 좌우에 배경색 페이드 마스크가 깔린다(컨텍스트).
- 좌측: `2173:105337` Rectangle 34987 — `#0A0A0A` (`fill_19UZTW`) → `--color-surface-base`, 1585×434 (히어로 영역과 공유).
- 우측 그라데이션류: `fill_M9ZKB4` = `linear-gradient(-90deg, rgba(10,10,10,0) 0%, rgba(10,10,10,1) 100%)` (히어로 우측 페이드); 칩/카드 캐러셀 페이드 `fill_EUXE7H` = `linear-gradient(-90deg, rgba(18,18,18,1) 0%, rgba(18,18,18,0) 100%)`(=`--color-surface` 18,18,18 페이드). → 토큰갭(그라데이션 토큰 미정의), 단 끝색 `#121212`는 `--color-shell-bg`.

---

## 6. 데이터/카피 텍스트 (그대로 기록)

- 1차 세그먼트: `취향관`(선택) · `피드`
- 카테고리 칩행 라벨(좌→우): `내 분야` · `프로그래밍` · `UXUI 디자인` · `업무 생산성` · `IT 기획` · `리더십`(**ACTIVE**) · `협업` · `분야 추가`
- 히어로 카피(컨텍스트): `Grabit이 엄선한 그랩` / 본문 큰 제목 `지금 당신은 원하던 삶을 살고 있나요? / 나답게 살기 위한 실리콘밸리 리더의 도전 이야기`
- 하단 섹션 제목: `프로덕트 디자이너가 많이 본 컨텐츠` (`Title 1/Bold` = Pretendard / 700 / 24px / lh130% / ls-2.5%, 색 `#FAFAFA`).
